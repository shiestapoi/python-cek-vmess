# v2ray_report.py — Python dependencies
#
# All standard-library packages (argparse, base64, concurrent.futures,
# datetime, html, ipaddress, json, os, re, shutil, socket, subprocess,
# sys, tempfile, threading, time, urllib.*) need no installation.
#
# Runtime system tools required (not pip packages):
#   curl   — for all HTTP geo-API calls
#   xray   — for proxy connectivity checks (optional; skipped if absent)
#
# ── Optional: offline MMDB lookup ────────────────────────────────────────────
# Enables --geo-provider offline and GeoLite2 mmdb fallback in auto/free mode.
# MMDB files are auto-downloaded from sapics/ip-location-db on first run.
maxminddb>=2.0.0

# ── Optional: OG preview image generation (generate_preview.py) ──────────────
# Captures an exact screenshot of the HTML report for social media previews.
# After install, also run: playwright install chromium --with-deps
playwright>=1.40.0